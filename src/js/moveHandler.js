class MoveHandler {
    constructor(gameState) {
        this._gameState = gameState;
        while (BOOL.TRUE){
            //Await user move. from ui hook
            //validate user move.
            //maybe some user interaction?
            //if ok return
            console.log("Found valid user move");
            break;
        }
    }

    provideValidMove() {
        return undefined;
    }
}
